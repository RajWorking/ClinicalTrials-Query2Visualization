# Copy to .env and fill in.
OPENAI_API_KEY=sk-...

# Set STUB_LLM=1 to bypass OpenAI and use hand-crafted plans for the 5 example queries.
# Useful for evaluating end-to-end without spending tokens.
STUB_LLM=0

# Optional: override the model used by the planner.
OPENAI_MODEL=gpt-4o-mini
